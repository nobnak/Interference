using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum ModelType { Quad = 0, Circle, Triangle, Cross, END }
public enum TextureType { Cyan = 0, Magenta, Yellow, END }